# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist.zip
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


# Deploy config — points at your server; keep out of git
deploy/deploy.conf

# .env holds only the public VITE_ keys (safe to commit — they ship in the
# browser bundle). Secrets like SUPABASE_DB_URL live in .env.local, which is
# ignored by the *.local rule above and must NOT be committed.

