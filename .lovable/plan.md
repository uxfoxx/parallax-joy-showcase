

# Remove SectionTransition Between Featured and Why Choose Us

## Change
**File**: `src/pages/Index.tsx` (line 37)

Remove the `<SectionTransition />` on line 37 — the one sitting between the FeaturedProducts section and the WhyChooseUs section. The two sections will flow directly into each other.

